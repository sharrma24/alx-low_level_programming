#include "main.h"


/**
 * _abs - computes the absolute value of a integer
 * @n:  check the intiger
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
